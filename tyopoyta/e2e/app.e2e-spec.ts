import { TyopoytaPage } from './app.po';

describe('tyopoyta App', () => {
  let page: TyopoytaPage;

  beforeEach(() => {
    page = new TyopoytaPage();
  });

//  it('should display welcome message', done => {
//    page.navigateTo();
//    page.getParagraphText()
//      .then(msg => expect(msg).toEqual('Welcome to app!!'))
//      .then(done, done.fail);
//  });
});
